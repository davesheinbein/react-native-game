// Renders the stick figure player
import React from 'react';
import { View } from 'react-native';

export function StickFigure() {
	// TODO: Render stick figure (SVG or View composition)
	return (
		<View
			style={{
				width: 40,
				height: 80,
				backgroundColor: 'transparent',
			}}
		/>
	);
}
